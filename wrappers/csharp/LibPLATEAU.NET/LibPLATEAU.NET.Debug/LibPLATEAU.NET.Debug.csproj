<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net6.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\LibPLATEAU.NET\LibPLATEAU.NET.csproj" />
    </ItemGroup>

	<!-- C#テストの実行のために下のコマンドが必要です。-->
	<!-- C++のビルドで出力された Plateauライブラリ(OSにより dll, so, dylib のどれか)とテスト用データを、このテスト用 C#プロジェクトにコピーします。-->
	<!-- コマンドは 2パターン書きます。上から Windows向け、 Linux向け(Mac含む)　です。-->
	<!-- C++でビルドしたライブラリの出力場所が両者で異なることに注意してください。-->
	<Target Name="PostBuild" AfterTargets="PostBuildEvent">
		<Exec Condition="$([MSBuild]::IsOSPlatform('Windows'))" Command="copy /Y $(ProjectDir)..\..\..\..\out\build\x64-Release\bin\*.dll $(TargetDir)&#xD;&#xA;&#xD;&#xA;xcopy /S /Y $(SolutionDir)..\..\..\data $(TargetDir)\data\" />
		<Exec Condition="$([MSBuild]::IsOSUnixLike())" Command="find $(ProjectDir)../../../../out/build/x64-Release -name *.so -or -name *.dylib | xargs -I {} cp {} $(TargetDir)" />
		<Exec Condition="$([MSBuild]::IsOSUnixLike())" Command="mkdir -p $(TargetDir)data/" />
		<Exec Condition="$([MSBuild]::IsOSUnixLike())" Command="cp -R $(ProjectDir)../../../../data/* $(TargetDir)data/" />
	</Target>

</Project>
